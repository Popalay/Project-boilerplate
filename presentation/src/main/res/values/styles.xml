<resources>

    <!--TODO Provide own app name-->
    <style name="Appname" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:windowBackground">@color/window</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
    </style>

    <style name="Appname.Toolbar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="Appname.Dialog" parent="Base.Theme.AppCompat.Light.Dialog">
    </style>

    <style name="Appname.Dialog.Progress">
        <item name="android:windowNoTitle">true</item>
    </style>

</resources>
