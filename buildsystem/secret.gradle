ext {
    releaseStoreFile = "../buildsystem/release.keystore"
    debugStoreFile = "../buildsystem/debug.keystore"

    //TODO change sign in information
    keyAlias = "keyAlias"
    keyPassword = "keyPassword"
    storePassword = "storePassword"
}